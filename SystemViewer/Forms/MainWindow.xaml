<Window xmlns:controls="clr-namespace:SystemViewer.Controls" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xcad="http://schemas.xceed.com/wpf/xaml/avalondock"
        xmlns:s="clr-namespace:SystemViewer.Designer"
       WindowStartupLocation="CenterScreen"
        Title="Active Diagram Designer"
        x:Class="SystemViewer.Forms.MainWindow"
       Height="700" Width="1024" >

    <Window.Resources>
        <ContextMenu x:Key="DiagramCanvasContextMenu">
            <MenuItem Header="Paste" Command="{x:Static ApplicationCommands.Paste}">
                <MenuItem.Icon>
                    <Image Source="../Designer/Images/Paste.png" Width="16"/>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="Select All" Command="{x:Static controls:DiagramCanvas.SelectAll}"/>
        </ContextMenu>
    </Window.Resources>
    <Grid x:Name="ControlGrid" Background="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="24"/>
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="1" Content="{StaticResource MyToolbar}"/>
        <xcad:DockingManager x:Name="dockingmanager" Grid.Row="2" Background="DarkGray" >
            <xcad:DockingManager.DocumentHeaderTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding IconSource}" Margin="0,0,0,0"/>
                        <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </xcad:DockingManager.DocumentHeaderTemplate>
            <xcad:LayoutRoot>
                <xcad:LayoutRoot.LeftSide >
                    <xcad:LayoutAnchorSide >
                        <xcad:LayoutAnchorGroup />
                    </xcad:LayoutAnchorSide>
                </xcad:LayoutRoot.LeftSide>
                <xcad:LayoutPanel>
                    <xcad:LayoutDocumentPaneGroup>
                        <xcad:LayoutDocumentPane/>
                    </xcad:LayoutDocumentPaneGroup>
                    <xcad:LayoutAnchorablePaneGroup DockWidth="150">
                            <xcad:LayoutAnchorablePane />
                    </xcad:LayoutAnchorablePaneGroup>
                </xcad:LayoutPanel>
            </xcad:LayoutRoot>

        </xcad:DockingManager>
        <Menu x:Name="MainMenu"  ItemsSource="{Binding MenuItems}" HorizontalAlignment="Stretch" Grid.Row="0"  VerticalAlignment="Top"   >
            <Menu.ItemContainerStyle>
                <Style TargetType="{x:Type MenuItem}">
                    <Setter Property="Command" Value="{Binding Command}" />
                    <Setter Property="CommandParameter" 
						Value="{Binding CommandParameter}" />
                    <Setter Property="Header" Value="{Binding Header}" />
                    <Setter Property="ItemsSource" Value="{Binding Items}"/>
                </Style>
            </Menu.ItemContainerStyle>
        </Menu>
        <!--<Canvas x:Name="MainCanvas" HorizontalAlignment="Stretch"  Grid.Row="1" VerticalAlignment="Stretch" Background="#FF3F443C" Grid.ColumnSpan="2" Margin="3,3,3,3"/>-->
        <StatusBar Grid.Row="3">
            <StatusBarItem Content="System Viewer"/>
        </StatusBar>
    
    </Grid>
</Window>
